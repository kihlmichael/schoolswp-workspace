# Skoatch API bearer token (Laravel Sanctum).
# Generate it from the Skoatch dashboard (https://skoatch.com/account/api)
# or via the CLI subcommand: tools.skoatch.cli login --email ... --password ... --device-name ...
SKOATCH_TOKEN=
