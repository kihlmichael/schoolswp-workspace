# Post 2871653 — Rank Math + Tags WordPress (saisie manuelle)

> Règle absolue : aucun push REST/SQL sur les meta Rank Math. Les valeurs ci-dessous
> sont à saisir dans la sidebar Rank Math du bloc-éditeur Gutenberg, puis enregistrer
> le brouillon.

URL preview : https://schoolswp.com/?p=2871653&preview=true

## Focus Keyword (Rank Math, champ principal)

```
fluentcrm automatisation
```

## Secondary Keywords (Rank Math Pro, sous le focus)

Une par ligne dans le champ "Secondary Keywords" (séparateur virgule selon ta version) :

```
fluentcrm workflow
automatisation email wordpress
fluentcrm tutoriel
fluentcrm pro
```

## SEO Title (Rank Math `Edit Snippet` → Title)

Cible 60 caractères max. 3 variantes, choisis celle qui te plaît :

1. `FluentCRM automatisation : 10 workflows que je fais tourner` *(58 c)*
2. `FluentCRM : 10 automatisations indispensables sur WordPress` *(58 c)*
3. `FluentCRM workflows : 10 automatisations que j'utilise vraiment` *(60 c)*

## Meta Description (Rank Math `Edit Snippet` → Description)

Cible 150-155 caractères. 3 variantes :

1. `10 automatisations FluentCRM que je fais tourner sur schoolsWP : welcome, panier abandonné, onboarding, re-engagement, LMS. Setup détaillé, free vs Pro.` *(154 c)*
2. `Mes 10 workflows FluentCRM testés sur WordPress : welcome, lead magnet, panier WC, LMS, scoring. Avec ordre d'implémentation et erreurs à éviter.` *(151 c)*
3. `10 automatisations FluentCRM essentielles sur WordPress : triggers, actions, conditions, free vs Pro. Le guide prescriptif avec captures et stack réelle.` *(154 c)*

## Open Graph (optionnel — laisser hériter du SEO Title/Description si pas besoin)

OG Title :
```
FluentCRM : 10 automatisations que je fais vraiment tourner sur schoolsWP
```

OG Description :
```
Welcome, panier abandonné, onboarding, re-engagement, LMS : 10 workflows testés avec setup détaillé, free vs Pro, erreurs à éviter.
```

## Tags WordPress (sidebar `Étiquettes` → ajouter)

À saisir un par un (Rank Math n'y touche pas, c'est la taxonomie native WP) :

- FluentCRM
- automatisation
- email marketing
- workflow
- WordPress
- CRM
- marketing automation
- newsletter

## Catégories

Catégories actuelles côté API : `[343, 1738]`.
Vérifier dans la sidebar que ces deux catégories correspondent bien au cluster Automation / CRM. Si pas, basculer.

## Vérifications avant publication

- [ ] Focus keyword saisi → Rank Math score affiche au moins **70+** (idéal 80-90)
- [ ] SEO Title contient le focus keyword en début
- [ ] Meta description contient le focus keyword
- [ ] OG image définie (Featured Image actuelle = `_thumbnail_id=2872843`)
- [ ] Pillar / Cornerstone Content : à activer si la fiche devient l'asset central du cluster Automation
- [ ] Schema : Rank Math doit générer le Article schema par défaut. Si FAQ block actif, ajouter manuellement un FAQ schema via Rank Math FAQ option.
- [ ] Lecture finale : aucun em-dash (`—`) ne doit apparaître dans le contenu final
- [ ] Lien affilié `schoolswp.com/fluentcrm/` présent + disclosure visible
- [ ] Test des liens internes ajoutés (5 nouveaux dans cette passe) : tous renvoient HTTP 200

## Maillage de retour à créer (après publication)

Une fois cette page publiée, **ajouter un lien retour** depuis :

1. `/fluentcrm-est-il-le-meilleur-crm-wordpress/` (rank actuel #8 sur "fluentcrm automation") → ancre type : *"Voir les 10 automatisations FluentCRM que je fais tourner"*
2. `/comparatif-groundhogg-vs-fluentcrm/` → ancre : *"10 workflows FluentCRM à mettre en place"*
3. `/fluentsmtp-avis/` → ancre : *"automatisations FluentCRM"*
4. `/ottokit-avis-automatisation-wordpress/` → ancre : *"workflows FluentCRM"*
5. `/fluentcart-gratuit-vs-pro/` → ancre : *"récupération panier abandonné FluentCRM"*

Boost mutuel attendu : page parente (rank 8) doit gagner 2-3 positions grâce au push topique de la nouvelle page.
